# Environment Variables for FactsScan Backend

# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/factsscan

# JWT Secret (Change this to a random string in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-12345

# Gmail SMTP Configuration
# You need to create a Gmail App Password:
# 1. Go to https://myaccount.google.com/security
# 2. Enable 2-Step Verification if not already enabled
# 3. Go to https://myaccount.google.com/apppasswords
# 4. Generate an App Password for "Mail"
# 5. Use that 16-character password below (without spaces)

SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password-here

# Example:
# SMTP_USER=yourname@gmail.com
# SMTP_PASS=abcd efgh ijkl mnop (remove spaces: abcdefghijklmnop)
